class AnimationDurations {
  AnimationDurations._();

  static const fast = Duration(milliseconds: 150);
  static const normal = Duration(milliseconds: 300);
  static const slow = Duration(milliseconds: 600);
  static const verySlow = Duration(milliseconds: 1000);
  static const splash = Duration(milliseconds: 1200);
  static const pageTransition = Duration(milliseconds: 350);
  static const shimmer = Duration(milliseconds: 1200);
  static const countUp = Duration(milliseconds: 1800);
  static const progressRing = Duration(milliseconds: 1500);
  static const autoScroll = Duration(seconds: 4);
}
